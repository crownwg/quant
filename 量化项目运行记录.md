---
title: A股量化项目运行记录
date: 2026-09-10
tags:
  - a股量化
  - akshare
  - 回测
  - 问题记录
aliases:
  - 量化运行记录
---

# A股量化项目运行记录

> [!info] 使用约定
> 每次运行、修改或排查项目之前，先读取本笔记，了解最近的数据源、环境和已知问题；完成后把新的问题和解决方式追加到本笔记。

## 当前项目

- 工作区：`C:\Users\FG\Documents\ChatGPT\量化`
- Python 环境：`.venv\Scripts\python.exe`
- 入口：`python -m quant.main`
- 研究策略：月度横截面动量，默认 120 个交易日、前 10 只、等权持有。

## 已记录问题与处理

### AkShare RemoteDisconnected

东方财富接口曾出现 `RemoteDisconnected: Remote end closed connection without response`。原因通常是数据源限流、网络连接被重置或接口临时不稳定，并不表示 AkShare 必须翻墙。

当前 `quant/data.py` 的处理方式：

1. 新浪日线接口优先。
2. 新浪失败后回退东方财富。
3. 东方财富请求最多重试 5 次，并逐步增加等待时间。
4. 多只股票请求之间默认等待 1 秒。
5. 支持 `data\000858.csv` 本地缓存，存在缓存时优先读取本地文件。

### VS Code 找不到解释器

在 VS Code 中选择 `Python: Select Interpreter` 后，如果列表没有出现项目环境，可选择 `Enter interpreter path`，手动指定：

`.venv\Scripts\python.exe`

当前环境中 AkShare 的入口文件位于：

`.venv\Lib\site-packages\akshare\__init__.py`

### 权重重置问题（已修正）

旧实现使用 `weights.replace(0, pd.NA).ffill()`，会把未入选股票的 0 权重当成缺失值，导致它们继承上个月仓位。现已改为 `weights.ffill().fillna(0.0)`：调仓日整行先保持全 0，再给当期入选股票分配 `1 / 持股数`，因此被剔除股票会正确归零。

## 数据用途

- `open`：模拟次日开盘成交，减少前视偏差。
- `close`：计算收益率、动量、均线和回撤。
- `volume`：做流动性过滤，排除成交量过低的股票。
- `equity_curve.csv`：策略净值曲线，不是原始行情数据。

## 后续运行检查

- [ ] 运行前先读取本笔记。
- [ ] 确认 `.venv` 解释器和依赖可用。
- [ ] 先检查本地 `data/` 缓存，再决定是否访问网络。
- [ ] 回测后检查权重总和是否不超过 100%。
- [ ] 将新问题、修复和回测结果追加到本笔记。

## 工作流程约定

用户要求：每次运行或修改量化项目之前，先读取本 Obsidian 笔记；完成后记录遇到的问题、采取的处理和验证结果。该约定适用于后续所有项目操作。

## 2026-09-11 策略检查

当前新增的选股因子已检查：动量、反转、均线趋势、均线突破、低波动、相对量能，以及可组合的多因子模型。Python 编译检查通过。

发现的注意事项：

- 当前已有回测结果总收益约 30.15%，年化约 5.78%，但最大回撤约 59.11%，风险很高，不能仅看正收益。
- 示例股票池只有 5 只，不能代表全 A 股横截面选股能力；需要扩大到几十至几百只并做样本外验证。
- `--strategy` 单因子与 `--factors` 多因子入口清晰，但不同因子窗口会导致前期 NaN，需确保预热期足够。
- 涨跌停过滤基于前复权收盘价涨跌幅，遇到分红除权日可能误判，正式研究应使用未复权价格或交易所涨跌停字段。
- `volume_trend` 只衡量相对放量，不代表绝对可成交；绝对流动性仍需配合 `--min-volume`。
- `--use-open` 已采用两根 K 线延迟来避免收盘信号使用此前开盘价的前视偏差；应保持开启并重新比较结果。

## 2026-09-11 择时参数纳入样本外验证（P2-2）

需求：上一轮的全样本网格显示「ma60 择时三项全改善」（收益 40.4%→61.9%、夏普 0.42→0.66、
回撤 −41.3%→−32.1%）——但这同样可能只是搜出来的。把择时参数一起丢进 walk-forward。

改动：
- `quant/grid.py`：combo 从 tuple 升级为 dict，新增择时三维（`timing` / `timing_lookback` /
  `timing_band`）；新增 `build_combos` / `combo_label` / `_timing_cfg` / `_exposure_getter`
  （按组合惰性构造敞口 + 缓存 + 按 `end_pos` 切片保证无前视）；`grid_search` 与
  `walk_forward_search` 接受 `timing_modes/timing_lookbacks/timing_bands/proxy`。
- `quant/main.py`：新增 `--grid-timing` / `--grid-timing-lookbacks` / `--grid-timing-bands`；
  拆出 `_resolve_proxy`（带 `args._proxy_cache` 缓存，避免几十组参数重复联网取指数）；
  预热期自动延长到最长候选择时窗口。
- 报告：网格报告新增「择时维度对比」图 + 组合标签列；walk-forward 报告的逐折表改为
  直接显示选中组合（含择时），并显示「各折选中的择时模式」统计。
- `tests/test_grid_timing.py`：19 项，全套 123 项通过。

实测（消费池 2020-01-01~2024-09-09，momentum/120/top15/use-open，代理 sh000932，
训练 504 日 / 测试 126 日，5 折，搜索空间 31 组）：

| 参数策略 | 样本外总收益 | 平均夏普 | 正收益折 |
|---|---|---|---|
| Walk-Forward 自适应（含择时） | −0.5% | 0.18 | 60% |
| 全样本最优固定（事后选参） | +20.7% | 0.80 | 80% |
| 默认参数（不择时） | −15.9% | −0.33 | 20% |

逐折选中：ma × 4、momentum × 1；窗口 40 日 × 4；滞回带 0 与 2% 各半。

结论：
1. 「某个参数点最优」是拟合的——事后选参高估 21.2pp（不扫择时时只高估 4.8pp）。
2. 「用不用择时、用哪一类」是结构性可信的——自适应选参样本外 −0.5% 好过不择时 −15.9%，
   差 15.4pp；5 折里 4 折选 ma。报告只讲结构性结论，不讲点估计。
3. 滞回带无信息量（0 与 2% 各半）→ 停止调 band，改为跨折参数集成（中位数/投票）。
4. 样本段警告必须一起交代：测试窗 2022-01~2024-09 恰是该池最差的一段，
   「择时样本外救回 15pp」是在熊市样本上取得的，别把 −0.5% 读成「择时能赚钱」。

命令：
```
.venv/Scripts/python.exe -m quant.main --pool 消费 --strategy momentum --top-n 15 --use-open \
    --benchmark sh000932 --timing-proxy 000932 --start 20200101 --end 20240909 \
    --walk-forward --fw-train 2 --fw-test 0.5 \
    --grid-lookbacks 120 --grid-topn 15 --grid-buffers 0 \
    --grid-timing off,ma,momentum,dual \
    --grid-timing-lookbacks 40,60,90,120,200 --grid-timing-bands 0,0.02
```

遇到的问题：无（`--grid-timing` 会强制把 `off` 补进搜索空间，
否则 walk-forward 永远得不出「不如不择时」的结论——这条已写进单测）。
依旧未解决：GitHub 推送被公司代理拒绝（需在本机网络手动 push）。

---

## 2026-09-11（续）不做单点择优：邻域平滑选参 + 全组合集成（P2-3）

**动机**：上一轮证明了「训练窗取 argmax」挑到的大概率是噪声（滞回带 0 与 2% 各折各半）。
那就别挑了，加两条不做单点择优的对照臂。

**改动**（`quant/grid.py` + `quant/main.py`）
- `_neighbor_map`：邻域 = 各维度在候选序列上相差 ≤ 1 档，**且择时模式必须相同**（关系对称，有单测锁）
- `smooth_scores`：参数曲面上的邻域均值滤波（纯函数）→ 再取 argmax，即「选最高的那一片」
- `ensemble_weights`：所有候选的**权重矩阵**等权平均（不是平均净值曲线——后者要 N 份资金各付一遍成本）
- `walk_forward_search` 输出从 3 条臂扩到 **5 条**：argmax / 邻域平滑 / 全组合集成 / 事后选参 / 不调参
- 报告：汇总表加「vs argmax」列，逐折表加「邻域平滑选中 + 三者收益对比」，净值图 5 条线，判定文案加「不踩尖峰更赚」
- `tests/test_wf_ensemble.py`：17 项；全套 **140 项通过**

**顺手抓到一个真 bug（P1 遗留）**：`score_cache` 原先只按网格候选预建，
而默认对照组用的是 `args.lookback`。于是 `--grid-lookbacks 60,90`（不含默认的 120）
会让 walk-forward 直接 `KeyError: 120`——只要把 120 写进候选就永远不会暴露。
已改为惰性缓存，并补了专门的单测。

**实测**（消费池 2020-01~2024-09，momentum/120/top15，5 折，31 组）

| 参数策略 | 样本外总收益 | 年化 | 平均夏普 | 正收益折 | 最差折回撤 |
|---|---|---|---|---|---|
| WF argmax 选参 | −0.5% | −0.2% | 0.18 | 60% | −15.5% |
| **WF 邻域平滑选参** | **+5.9%** | 2.2% | **0.49** | 60% | −17.9% |
| 全组合集成 | −1.7% | −0.6% | 0.01 | 40% | **−8.3%** |
| 全样本最优固定（事后选参） | +20.7% | 7.6% | 0.80 | 80% | −8.4% |
| 默认参数（不择时） | −15.9% | −6.5% | −0.33 | 20% | −17.3% |

逐折（邻域 − argmax）：[−9.3, **+10.9**, 0, 0, +4.7] pp。

结论：
1. **邻域平滑赢得很不平均**：5 折里 2 折与 argmax 完全相同、1 折输 9.3pp、1 折赢 10.9pp。
   +6.4pp 几乎全来自第 2 折——argmax 选中 momentum(90日)（跨模式、属整体平庸的 momentum 组）
   样本外 −5.7%，平滑抹掉落回 ma 附近拿到 +5.2%。
   → 平滑的真实作用是**防止一次跨模式翻车**，降低选参方差，不是提高期望收益。
2. **集成的价值在回撤不在收益**：收益平庸（−1.7%），最差折回撤 −8.3% 是五条臂最小。
   不把资金押在一次选择上，单折踩雷幅度被摊薄。
3. **哪怕 +6.4pp 是运气，换掉 argmax 仍然是对的**——原理正确的做法 ≠ 结果保证。
4. 下一步：集成的改良（先按邻域平滑排序取前 K 组再平均，或按模式组均值加权），
   现在是无差别平均全部 31 组，把最差的 momentum 组也等权持有了。

**文档** README 实测表改五臂 + 新增 9.2 节；SKILL.md 新增 P2-3（坑 7/8/9 + 实跑表 + 三条诚实判断 + 必测项）。

---

## 2026-09-11（续二）集成的改良：只在好区域内集成（P2-3 后半）

**动机**：上一轮的「全组合集成」是无差别平均全部 31 组，把已知最差的 momentum 组
（该组 31 组里平均夏普最低、半数窗口亏钱）也等权持有了，被拖累成「收益平庸、只换来回撤最小」。
本轮改为**先按邻域平滑分排序、只在前 K 组内等权平均**（`--wf-ensemble-k 3,5,10,0`）。

**改动**（`quant/grid.py` + `quant/main.py`）
- `parse_ensemble_ks` / `ensemble_arm_keys` / `ensemble_arm_label`：K 列表 → 多条并列臂（`ens3`/`ens5`/`ens10`/`ensall`）
- `walk_forward_search(..., ensemble_ks=...)`：逐折按**平滑分**降序取前 K 组集成；
  各组合权重算一次所有 K 共用（`w_cache`）；`fold_rows` 新增 `test_return_ens<k>`
- 汇总表 `vs argmax` 列覆盖所有 `ens*` 臂；`ensemble_note` **改报 K 的整段区间**（不再是「最优 K」单点）
- 新增 `k_note`（K 极差 + 明确提示「别事后挑最好的 K」）与 `ens_note`
  （最窄集成臂各折持有哪些择时模式 → 回答「集成是不是只押了一个模式」）
- 逐折表新增 `ens_top` / `ens_top_modes`（最窄臂实际持有的组与模式）
- 报告：新增「集成度 K 的敏感度」图组（`#kcurve`/`#knote`）、净值图按臂数动态配色、卡片文案
  「最优 K」→「并列多条 K，此为其一」
- CLI：`--wf-ensemble-k`（默认 `3,5,10,0`）；汇总打印加「最差折回撤」列
- `tests/test_wf_ensemble.py` 扩到 34 项；全套 **151 项通过**

**顺手抓到的真 bug（本轮实跑才暴露）**：`parse_ensemble_ks` 对自己**不幂等**。
`main.py` 先解析一次 `--wf-ensemble-k`，把 `(3,5,10,None)` 传给 `walk_forward_search`，
后者内部又解析一次 → `int(None)` 抛 TypeError。
单测只喂过字符串所以没暴露，`--walk-forward --wf-ensemble-k 3,5,10,0` 一跑就崩。
已修（显式处理 `None`）并补幂等性 + 非法值 + 「直喂元组」三组测试。

**实测**（同一段测试窗，唯一差别是「怎么选参数 / 集成度」）

| 参数策略 | 样本外总收益 | 年化 | 平均夏普 | 最差折回撤 | 正收益折 |
|---|---|---|---|---|---|
| WF argmax 选参 | −0.5% | −0.2% | 0.18 | −15.5% | 60% |
| WF 邻域平滑选参 | +5.9% | 2.2% | 0.49 | −17.9% | 60% |
| **集成 K=3** | **+16.7%** | 6.2% | **0.73** | −8.4% | 80% |
| 集成 K=5 | +12.6% | 4.7% | 0.67 | −8.4% | 80% |
| 集成 K=10 | +4.1% | 1.6% | 0.32 | −8.4% | 60% |
| 集成 K=31（全组合，上轮做法） | −1.7% | −0.6% | 0.01 | **−8.3%** | 40% |
| 全样本最优固定（事后选参） | +20.7% | 7.6% | 0.80 | −8.4% | 80% |
| 默认参数（不择时） | −15.9% | −6.5% | −0.33 | −17.9% | 20% |

最窄集成臂（前 3 组）15 个席位：**ma 10 席、dual 4 席、off 1 席、momentum 0 席**。

结论：
1. **唯一能下定量结论的是 K 的单调性**：16.7% → 12.6% → 4.1% → −1.7%，
   夏普 0.73 → 0.67 → 0.32 → 0.01，正收益折 80/80/60/40，**无锯齿**。
   若平滑分排序是噪声，这条曲线应该是平的、抖的。它单调且陡 ⇒
   参数曲面**顶部区域整体优于底部**（区域级结论，比任何点估计可信）。
2. **但 K 取多少没被验证**：极差 18.4pp，且 K 越小越占便宜是**结构性**的
   （K=1 就是平滑选参、再上一格就是事后选参 +20.7%）。**别把 K=3 当结论。**
3. **最稳、可放心用的一条：回撤控制与 K 无关**。K=5/10/31 最差折回撤都是 −8.4%/−8.3%，
   argmax −15.5%、平滑 −17.9%、默认 −17.9% ⇒「只要集成，最差折回撤就减半」。
4. **「集成」不是万能药，集成什么决定集成的价值**：同为集成，
   无差别平均 31 组 −1.7% vs 平滑分前 3 组 +16.7%（收益与回撤同时改善）。
5. **邻域平滑这次 +6.4pp 仍几乎全来自第 2 折**，作用仍是防一次跨模式翻车、降低选参方差。
6. **发现一个必须交代的结构性偏差**：`off` 在候选里只有 1 个代表，邻域只有自己
   ⇒ 平滑对它是恒等、永不被稀释；而 ma/momentum/dual 各组只有 4~6 个邻居。
   即**平滑偏袒 `off`（不择时）**。而实测 `off` 在最窄集成臂仅 1/15 席、argmax 里 0/5 折
   ⇒ 结论是**逆着偏差**得到的，更可信。已写进 README 已知坑 19 与 SKILL.md 坑 8b。

**文档** README 9.2 全部改写（三方案表 + 8 臂实测表 + 逐折表 + 持有构成表 + 7 条诚实读法 +
复现命令）；已知坑新增 17（解析幂等）、18（K 是事后选择入口）、19（off 邻域偏袒）；
洞察 7 补「选参方式与集成度同样会被事后挑选」+「K 单调本身是信息」；
路线图新增已做项 + 改写仍未做项（按模式加权/指数加权，仍需并列报 K）。
SKILL.md P2-3 改写（三条方案、坑 7/8/8b/9/10、8 臂实测表、四条诚实判断、K 专属必测项）。

仍存在：GitHub 推送被公司代理拒绝（需在本机网络手动 push）；3 个泄露 PAT 待撤销。

---

## 2026-09-11（续三）Web 控制台：把仓位管理搬到页面上

**背景**：用户提出「不想输入命令，想在页面上输入，然后页面出结果」。
排查发现 `quant/web.py`（FastAPI）+ `quant/templates/index.html` **早就存在、服务也在跑**，
但**页面上一个仓位管理（择时）的开关都没有**——正好是用户上一轮最关心的那块。
所以本轮不是从零做，而是把已有 Web 控制台补齐到与 CLI 能力对齐。

**改动**

`quant/web.py`（195 → 约 300 行）
- `RunParams` 增 11 个字段：仓位管理 7 个（`timing` / `timing_lookback` / `timing_band` /
  `timing_min_exposure` / `timing_max_exposure` / `vol_target` / `timing_proxy`）
  + 样本外 4 个（`walk_forward` / `fw_train` / `fw_test` / `grid_timing`）
- 抽出 `_build_cmd(p, prefix)`：表单 → 命令行的翻译独立成函数，便于单测覆盖
- 新增 `_parse_wf_results`：walk-forward 走另一套输出文件（`wf_summary.csv` / `wf_folds.csv`），
  返回各条臂汇总 + 逐折明细 + 报告路径
- 新增 `_records(df)`：`to_dict('records')` 会留 NaN，FastAPI 序列化直接抛
  「Out of range float values are not JSON compliant」；改走 `to_json` → `json.loads`
- 新增 `/report/{file}`（内嵌 HTML 报告）与 `/download/{file}`（CSV 下载），
  用 `_safe_file` 做路径穿越防护 + 后缀白名单
- 单次回测返回体新增 `mode` / `exposure` / `plan` / `report_file` / `cmd`

`quant/templates/index.html`（单文件前端）
- 新增两个可折叠区块：**仓位管理**（择时模式 + 均线长度 + 滞回带 + 仓位上下限 +
  波动率目标 + 代理指数）、**样本外验证**（开关 + 训练/测试窗 + 要扫的择时模式）
- 页面用**百分数**填写（滞回带 %、仓位 %、波动目标 %），提交时除以 100 转成后端口径
- 结果区新增：启用择时后显示**平均仓位 / 在场时间 / 切换次数**；
  样本外模式新增 `renderWfResult`——结论块 + 各做法对比表 + 逐折明细表 + 报告/CSV 链接
- 预设按钮加 3 个：消费·加择时 ma60、沪深300·加择时 dual、消费·样本外验证
- **颜色改为 A 股习惯（红涨绿跌）**：原来指标卡、年度柱状图、月度热力图用的都是欧美口径
  （正=绿、负=红），已全部翻转。注意回撤曲线仍用红色——那是「风险」不是「涨跌」

`启动网页.bat`（新增，931 字节，纯 ASCII + CRLF + GBK 编码）
- 双击启动服务并自动开浏览器；已在运行则直接开浏览器
- 服务跑在独立的 `quant-web` 窗口，关掉主窗口不影响服务

`tests/test_web.py`（新增，22 项）→ 全套 **173 项通过**（原 151）

**本轮抓到的真问题**

1. **样本外网格会爆炸（严重）**：`walk_forward=True` 时若不传
   `--grid-lookbacks/topn/buffers`，main.py 会套用 CLI 默认的 4×3×2=24 组选股网格，
   再乘择时组合（4 模式 × 5 窗口 × 2 带 = 31）→ **最多 700+ 组**，一次要跑几小时。
   修法：把选股网格钉死在页面上填的那一组，只让「择时」这一维去扫 → 31 组、约 20 秒。
2. **NaN 无法 JSON 序列化**：`pd.read_csv(...).to_dict("records")` 保留 NaN，
   FastAPI 序列化抛 `Out of range float values are not JSON compliant`。
   `wf_folds.csv` 里有大量可选列（`test_return_ens5` 等），必然踩到。改用 `to_json`。
3. `--timing-proxy` 在仓位管理与样本外两处重复拼接 → 合并为一处。

**实测（页面端到端）**

| 场景 | 参数 | 耗时 | 结果 |
|---|---|---|---|
| 单次 + 择时 ma | 消费池 momentum 120 / top15 / ma(60日, 带2%) | 15.0s | 总收益 +61.9% / 年化 +10.8% / 夏普 0.66 / 回撤 −32.1%；平均仓位 47.0%、切换 20 次 |
| 单次 + 择时 dual | 同上但 dual(120日) | 14.9s | 总收益 +29.3% / 夏普 0.40 / 回撤 −31.2%；平均仓位 29.8%、切换 23 次 |
| 样本外 | 网格钉死 + `--grid-timing off,ma`（11 组） | 22.1s | 8 条臂全部产出；ens3 +7.8% 最优、最差段 −8.4%（对照 argmax −17.9%） |

dual 的平均仓位（29.8%）明显低于 ma（47.0%），与 dual「均线 + 斜率双确认、出场更早」的
设计语义一致——这是页面端参数接线正确的一个旁证。

安全验证：`/report/../../../Windows/win.ini`、`..%2F..%2FREADME.md`、`quant/web.py`
均返回 404。

仍存在：GitHub 推送被公司代理拒绝（需在本机网络手动 push）；3 个泄露 PAT 待撤销。

---

## 2026-09-11（续四）调仓清单上页面 —— 顺手挖出三个真 bug

**动机**：上一轮把仓位管理搬上页面后继续推进「从看回测 → 能实操」：
页面上还看不到「我明天该买什么、该卖多少」。CLI 里 `--today` / `--holdings`
早就有这个能力（项目根目录还躺着用户的 `my_holdings.csv`），只是没接到页面上。

**改动**

`quant/main.py`（修 bug）
1. **打印清单时崩溃（严重）**：`r['shares']` —— 该列不存在，真实列名是 `delta_shares`。
   一执行就 KeyError，**进程以退出码 1 结束**。CLI 下只表现为「清单没打印出来」
   （CSV 其实已正确写盘，所以从现象很难反推）；Web 下 `proc.returncode != 0`
   直接判失败，功能等于不可用。顺带把股数改用 `abs(delta_shares)`
   （方向已由 action 表达，负数读数反直觉）。
2. **持仓里「池外股票」被静默吞掉**：`if pd.isna(price): continue` ——
   用户持仓里不在当前池价格表内的股票会直接消失，会被误读成「这只不用动」。
   现在收集 skipped 并显式告警：「以下持仓因取不到价格而无法计算，需单独处理」。

`quant/web.py`
- `RunParams` 加 `holdings_text` / `plan_date`
- 新增 `_materialize_holdings()`：持仓文本 → CLI 要的 CSV。留空回落 `my_holdings.csv`；
  解析宽容（逗号/空格/分号/制表符、`#` 注释、代码补零、负数跳过）
- 新增 `/api/plan` 独立入口（main.py 的 holdings 分支是独立 return、不产出净值曲线，
  所以清单与完整回测不能在一次调用里兼得）
- `_build_cmd` 加 `plan_only` / `holdings_path`；普通回测**绝不带** `--holdings`
- 新增 `_read_csv_codes()`：读 CSV 时补回代码前导零
- `_records` 重写（原因见下）

`quant/templates/index.html`
- 新增「调仓清单 · 我该买/卖多少」区块 + 独立按钮
- 抽出 `collectParams()` / `paramsSummary()` 供两个入口共用
- 新增 `renderPlan()`：汇总「要买 N 笔 / 合计 X 元」+ 明细表 + 跳过提示

**本轮挖出的三个真 bug**（前两个是既有代码，第三个是我上一轮自己引入的）

1. **`r['shares']` KeyError** → 退出码 1（如上）。
2. **持仓池外股票静默消失**（如上）。
3. **股票代码被截断**：`000858` 显示成 `858`、`002714` 变成 `2714`。
   根因链：上一轮为修 NaN 把 `to_dict('records')` 换成 `df.to_json()`，
   而 `to_json` 会把「看起来像数字的字符串」列自动转成数字。
   **而且第一轮还修错了地方**：我在输出端（`_json_safe`）逐格判类型，重测仍显示 `858`；
   真正的原因是**读入端** —— `pd.read_csv` 就把 `000858` 推断成整数 858，
   进到 `_records` 时数据已经错了。
   最终修法：`_read_csv_codes()` 读入时 `astype(str).str.zfill(6)`，
   同时保留 `_json_safe` 不动字符串。**两端都要修，只修一端无效。**
   → 教训：**先确认数据在哪一步变错的，再决定修哪里**；输出端做类型清洗前，
   要问一句「它进来的时候本来就是对的吗」。

**测试** `tests/test_daily_plan.py`（新增 10 项）+ `tests/test_web.py`（22 → 32 项）
→ 全套 **193 项通过**（原 173）
- `test_daily_plan.py` 专门锁**列名约定**（`delta_shares` 必须在、裸 `shares` 必须没有）
  —— 这类「列名拼错」的 bug，纯数值测试抓不到
- `test_read_csv_codes_restores_leading_zeros` 锁读入端补零
- `test_records_preserves_zero_padded_codes` 锁输出端不动字符串

**实测（页面端到端 `/api/plan`）**

输入持仓「600519,100 / 000858,1000 / 000001,5000 / 002557,300」，消费池 momentum top15：

```
建仓  000729 ~ 605499   共 15 笔
清仓  000858  现持 1000 -> 目标 0   变动 -1000  金额 104,870
清仓  600519  现持  100 -> 目标 0   变动  -100  金额 127,539
跳过  000001、002557（不在消费池的价格表内）
代码位数：[6, 6, ...]  全部 6 位 ✓
```

仍存在：GitHub 推送被公司代理拒绝（需在本机网络手动 push）；3 个泄露 PAT 待撤销。

---

## 2026-09-11（续五）数据体检 + 「用最新数据」上线 —— 顺带修掉一个「点了有反应」的 bug

### 起因：默认结束日是 2024-09-09

用户要「能用的调仓清单」。但结束日期默认 `20240909`，不改它直接生成清单，
拿到的是「假如当时是 2024-09-09 该买什么」——回测跑得通、清单很漂亮，
但完全不是今天的操作建议。**这是整个工具最容易踩的坑，而且踩了不会报错。**

所以先把前置条件验证掉：**数据到底能拉到哪天**。

### 数据现状核查

| 项 | 结果 |
|---|---|
| `data/` 规模 | 503 个文件、82MB |
| `data/600519.csv` 最后一行 | **2026-09-10**（昨天） |
| `data/index_sh000932.csv` 最后一行 | **2026-09-10** |
| `data/cons_000932.csv` | 36 只成分股 |
| `_coverage.json` | 115 条，存的是**上市基线日**，**不是**数据截止日 |
| 个股缓存新鲜度 | **参差不齐**：抽样 30 只里 15 只停在 `2024-09-09` |

结论：**数据源能拉到最近交易日，但缓存本身是斑驳的。**
原因是缓存更新只覆盖「本次真正用到的那几只」——池里没被选中的标的
会一直停在旧日期，而它们照样会进入候选池。

### 新增 `--refresh-data`：只更新数据，不跑回测

放在 `main()` 里 `fetch_start` 算完之后分叉（刚好需要 codes + fetch_start）。
复用 `load_panel`（它内部会写缓存），不另写一套拉取逻辑。

```bash
.venv/Scripts/python.exe -m quant.main --pool 消费 --codes 002557 \
    --refresh-data --start 20230101 --end 20260910
```

```
▶ 增量更新 37 只标的的数据 → 目标 20260910（本地无缓存 0 只，将从 20220406 起拉全量）

  数据体检（共 37 只）
    最新截止日 : 2026-09-10  ✓ 已到目标
    取不到数据 : 0 只
    明显落后   : 0 只  ✓
```

- 消费池 36 只：**16 秒**补完
- 体检判定用「落后 > 5 个自然日」才算真旧 —— 差 1~2 天多半是停牌或
  不同收盘日，每天几十只这种票，全列出来只会淹没真正的问题
- 底部提示文案区分「多为长期停牌（正常）」与「整池大面积落后（网络/代理问题）」

顺带把 `cache_freshness()` 放进 `quant/data.py`（缓存逻辑本来就在那儿），
而不是留在 `main.py` —— 否则 Web 端为了读个 CSV 要 import 整个回测主模块。

### 页面：三层防护

1. **打开页面自动体检**：`POST /api/data_freshness`，只读本地 CSV、**不联网**，
   所以可以随加载随便调。
2. **状态文案 + 一键对齐**：偏离超 5 天标黄，并提供「用最新数据」按钮。
3. **结果区顶部再弹一条横幅**：点了按钮后用户视线在右边，只把警告留在左侧小字里会被忽略。

新增 `POST /api/refresh_data`（同步执行，超时上限 60 分钟）。

判定要**对两个方向都敏感**，方向不同、后果不同：

| 情况 | 后果 |
|---|---|
| 结束日**远早于**数据最新日 | 拿到的是「历史回放」清单（默认值就落在这里）——**更危险** |
| 结束日**远晚于**数据 | 末尾一截没有数据，属于「该点更新数据」 |

### 挖出的 bug

**1. `--pool` 与 `--codes` 被页面写成二选一（真 bug）**

`main.py` 的语义是两者取并集（`manual + pool_codes`），页面写成了
`if pool: ... else: codes`。后果：**洽洽食品（002557）加不进来**。
核实了一遍——它不在中证主要消费（000932，36 只）、可选消费（000989，140 只）、
白酒（399997，17 只）、沪深300（300 只）的**任何一个**成分股名单里，
只能靠手动代码补。已修 `_build_cmd` 与 `_resolve_codes`（合并去重），
`codesField` 恒常显示并改名为「额外补充代码（可选，与股票池取并集）」。

实测（更新前 002557 末行 `2024-09-09`，1320 行）：

```
n_codes=37  with_data=37  latest=2026-09-10  usable=True
更新后 002557 末行 2026-09-10，1805 行 ✓（+485 行）
耗时 40.6 秒（含两年缺口全量回补）
```

**2. 预设按钮全部失效（既有 bug，一直没人发现）**

页面那排预设按钮调 `fillPreset()`，里面写的是 `$('lookback').value = lookback`。
但 `top_n` / `lookback` / `rebalance` / `benchmark` 这四个 input
**只有 `name` 没有 `id`**，`$()` 返回 `null`，赋值当场抛 `TypeError`：

```js
$('pool').value = pool;           // OK
$('pool').dispatchEvent(...);     // OK
$('strategy').value = strategy;   // OK
$('lookback').value = lookback;   // ← 抛异常，后面全不执行
```

后果：`top_n` / `rebalance` / `benchmark` 一个都没设上，
`fillTimingPreset` 里的择时参数**从来没生效过**
（异常在其内部抛出并向上传播）。之所以长期没被发现：
**池子和因子确实切了，按钮「有反应」。**

修法两处一起改：
- 补齐四个 `id`
- `fillPreset` / `fillTimingPreset` 改走 `document.forms.runForm`（按 `name` 取字段）。
  后者更关键：按 `name` 取，字段一定存在；按 `id` 取，漏写一个就是静默失效。

**这是我怎么发现的**：给新 UI 做静态检查时，顺手比对「JS 引用的 `id` vs HTML 定义的 `id`」，
报出 4 个缺失。原本只想确认自己没写错，结果翻出个旧账。

### 测试 193 → 217 项

- `tests/test_web.py` 32 → 50 项
  - `--pool`/`--codes` 必须能同时传且取并集
  - `--refresh-data` 命令**只带这一个 flag**，不掺择时/样本外/持仓
  - `data_freshness` 两个方向都敏感、1~5 天容差、早退分支字段齐全、
    停更与无缓存分开报、池名错给可读提示
  - **5 条前端 DOM 契约静态检查**（见下）
- `tests/test_data_cache.py`（新增 6 项）：缺文件的代码也要出现在结果里、
  单个缓存损坏不影响整池、`858` 要对上 `000858.csv`、只读 `date` 列
- 全套 **217 项通过**

**新增的 5 条静态契约测试**（这类错误在浏览器里不报错，只表现为「点了没反应」，
靠人眼 review 基本抓不到）：
1. JS 引用的每个 `id` 必须在 HTML 里存在
2. JS 读的每个表单字段必须存在
3. `onclick` / `onsubmit` 指向的函数必须已定义
4. 前端 `fetch` 的每个 `/api/*` 必须在 `web.py` 里注册
5. 体检 UI 的元素齐全

写这几条时先用 Node `--check` 验了内联脚本语法、比对了标签配平
（`div` 104 开 / 104 闭），再把检查逻辑固化成测试。

### 端到端实测（页面 `/api/plan`，基于最新数据）

```bash
curl -X POST http://127.0.0.1:8765/api/plan -d '{
  "pool":"消费","codes":"002557","strategy":"momentum","top_n":15,
  "start":"20230101","end":"20260910","timing":"ma","timing_lookback":60,
  "timing_band":0.02,"holdings_text":"600519,100\n000858,1000\n002714,500\n000001,3000"}'
```

15.9 秒、退出码 0，17 条清单：14 笔建仓 + 3 笔清仓（000858/002714/600519），
正确告警 `000001` 取不到价格。

### 仍未解决

- **GitHub 推送被公司代理拒绝**（`HTTPS_PROXY=http://127.0.0.1:59277`
  拒 git 的 HTTPS CONNECT 隧道，502）。需在本机网络手动 push：
  `git push -u "https://crownwg:<PAT>@github.com/crownwg/quant.git" main`
- **3 个泄露的 PAT 待撤销**
- 页面仍是**同步执行**，长任务会把请求挂住

---

## 2026-09-11 续：Chrome 无头截图验证通过，当前服务与未竟事项

### 当前状态（可续接点）

- **Web 服务在跑**：`127.0.0.1:8765`，PID `32528`。
  - 改 `quant/web.py` **必须重启**；改 `quant/templates/index.html` 不用重启。
  - 重启方式：任务管理器结束 PID 32528 的 Python，然后在项目根目录执行
    `.venv/Scripts/python.exe -m quant.web`，或双击 `启动网页.bat`。
- **工作区干净**：`git status --short` 为空。
- **最新本地提交**：
  ```
  7ecc435 chore: 根目录 _* 临时文件统一忽略
  49500c4 chore: 忽略一次性验证脚本
  930ad15 chore: .pytest_tmp/ 加入 gitignore
  28b47d0 feat: 数据体检上页面（用最新数据 / 更新数据）+ 修预设按钮全失效
  ```
- **测试 217 项全过**（上次重跑时间：2026-09-11 18:46 左右）。
- **数据体检 / 更新数据 / 用最新数据** 已经部署并端到端验证：
  - 消费池 36 只 `--refresh-data` → 16 秒补齐到 `2026-09-10`
  - 消费池 + 002557 → 40.6 秒，002557 由 2024-09-09/1320 行 → 2026-09-10/1805 行
  - 页面 `/api/plan` 基于最新数据 15.9 秒，14 建仓 + 3 清仓，正确告警 000001

### 无头截图验证结果

用 Chrome `--headless=new` 截了页面 1500×2000，文件在：
`C:/Users/FG/AppData/Local/Temp/qshot/page.png`（127 KB）。

看到的关键信息：
1. 左上角黄色警告框正确显示：  
   **「结束日 2024-09-09 比数据最新日 2026-09-10 早了 731 天：这份清单是历史回放……」**
2. 警告框内两个按钮 **「用最新数据」「更新数据」** 可见。
3. 额外补充代码框下方提示：**「002557（洽洽食品不在任何指数成分股里）」**。
4. 右上角「服务在线」badge 正常。
5. 左侧表单元素完整，折叠面板正常。

结论：**前端渲染与数据体检文案都正常，这一轮可交付。**

### 建议的下一步（优先级排序）

**P0：用户侧收尾**
1. **手动 push**：在本机（非公司代理环境）执行
   `git push -u "https://crownwg:<新 PAT>@github.com/crownwg/quant.git" main`
2. **撤销 3 个泄露的 PAT**（此前已暴露在 Bash 输出里）。

**P1：体验 / 防呆**
3. **默认结束日是否改成最新交易日？**
   - 现在默认 `20240909`，靠黄色警告框兜底；
   - 若改默认，则历史回测区间变短，首次打开页面看到的指标会变；
   - 折中方案：保持默认不变，把警告框改成**更明显**（如红色）或打开时自动弹一次模态框。
4. **数据更新的 loading 反馈**：点「更新数据」后请求同步挂着，页面没有任何 loading，
   用户可能以为卡死。可加一个全局遮罩 + 进度条（先读 `stdout` 的进度行）。
5. **「一键今日清单」按钮**：自动把结束日设最新 + 切到调仓清单模式 + 运行，
   减少 3 次点击。

**P2：技术债**
6. **把同步回测改成异步**：目前长任务会挂住 HTTP 请求。
   可行路径：FastAPI `BackgroundTasks` + SSE 进度推送 / 前端轮询。
7. **清理根目录临时产物**：`_today_*`、`_upd_*`、`_skill_*`、`_fr.json`、`.pytest_tmp`
   还在（沙箱删除被限流）。已在 `.gitignore` 里，不会误提交。
8. **把 Chrome 无头截图做成回归测试**：
   - 脚本已验证可用，可把它封装成 `tests/visual_smoke.py`；
   - 但依赖本机 Chrome/Edge，CI 上可能跑不了，可先放本地手动触发。

**P3：文档 / 沉淀**
9. 更新 README 里的「Web 控制台」截图或 GIF（等 UI 稳定后再做）。
10. 把前端 DOM 契约检查 skill 正式发布到 WorkBuddy（目前只写到本地 skill 文件）。

### 下次接手的快速入口

1. 先确认服务：
   ```bash
   cd "C:/Users/FG/Documents/ChatGPT/量化"
   curl -s http://127.0.0.1:8765/api/health
   ```
2. 看运行记录最新条目（就是本段）。
3. 改代码前先看 `MEMORY.md` 里 **Web 控制台的坑**。
4. 改完后重启服务 + 跑 `pytest` + Chrome 截图一条龙。

---

## 2026-09-14（续）补上长任务全局遮罩 + 清理临时产物

### 接手上轮

周末沙箱重置，Web 服务停了；根目录还堆着上轮的 `_today_*` / `_upd_*` / `_skill_*` 产物。
按上次续接点的优先级，先挑 **P1-4：长任务没有 loading 反馈，用户会以为页面卡死**。

### 发现：不是完全没有反馈

打开 `quant/templates/index.html` 后才发现，「运行回测」「生成调仓清单」已经有
结果区内的 spinner；「更新数据」也有按钮文字 + 左侧 freshbar 文字提示。
所以问题主要是 **「更新数据」的反馈太弱**（只是按钮和一行小字），40 秒的长任务里
用户仍可能觉得「是不是卡了」。

### 加的改动

**全局遮罩**（纯前端，不改后端，零回归风险）：
- 新增 `<div id="overlay">` + `.overlay` / `.overlay-card` / `.overlay-title` / `.overlay-sub` 样式；
- 新增 `showOverlay(title, sub)` / `hideOverlay()`；
- 在三个长任务入口统一接入遮罩：
  - `onSubmit`（运行回测 / 样本外验证）：`showOverlay` + `hideOverlay`
  - `onRunPlan`（生成调仓清单）：`showOverlay` + `hideOverlay`
  - `onRefreshData`（更新数据）：`showOverlay` + `hideOverlay`
- 用深色毛玻璃背景、卡片居中、带 spinner + 标题 + 副文案；
  文案按任务区分，例如更新数据明确写「消费池约 16 秒，含多年缺口约 40 秒」。

### 验证

1. **静态检查**：JS 语法通过、所有 `$('id')` 引用都有对应 HTML 元素。
2. **测试**：
   - 全套 **218 项通过**；
   - 新增 `test_global_overlay_present_and_wired`：
     - 确认 `#overlay` / `#overlayTitle` / `#overlaySub` 存在
     - 确认 `showOverlay` / `hideOverlay` 函数存在
     - 确认三个入口都调了 `showOverlay(`，且 finally 都调了 `hideOverlay()`
3. **视觉验证**：强制显示遮罩后 Chrome 无头截图，确认卡片居中、spinner 旋转、
   文案正确（截图存 `C:/Users/FG/AppData/Local/Temp/qshot2/overlay.png`）。

### 清理

本轮同时把上轮遗留产物全部清掉：
- `_skill_check.py` / `_skill_verify.py` / `_skill_tmp_check.js` / `_fr.json`
- `.pytest_tmp/`
- `_today_*.csv` / `_today_*.html`
- `_upd_*.csv` / `_upd_*.html`
- 本次验证产生的 `_overlay_preview.html`

根目录回到干净状态。

### 仍未解决（优先级不变）

- **P0**：手动 push + 撤销 3 个泄露的 PAT（用户侧）
- **P1**：默认结束日是否改到最新交易日（体验决策）
- **P1**：「一键今日清单」按钮
- **P2**：同步回测改异步（根源解法，但比遮罩大得多）
- **P2**：把 Chrome 无头截图做成回归测试
- **P3**：README 截图 / GIF、发布前端契约 skill

### 当前状态

- 服务：`127.0.0.1:8765` 已重新拉起（PID 待查）
- 测试：**218 项通过**（原 217 + 1）
- 工作区：2 个文件改动（`quant/templates/index.html` + `tests/test_web.py`）

---

## 2026-09-14（续）因子评价（IC/IR）上页面

### 背景

这是 P1 第一项——把 CLI 里已实现但 Web 没暴露的能力放出来。
现在页面上跑的策略是「挑出来的」，但**没有独立证据说明这个因子真的有效**。
回测净值涨了，可能是：因子有效 / beta 运气 / 过拟合。
IC/IR 检验回答的是：横截面上分数高的股票，未来是否真的跑赢分数低的。

### 后端

- 新增 `FactorEvalParams` 入参模型（股票池 / 手动代码 / 因子列表 / horizon / 分组数 / 各因子窗口）。
- 新增 `POST /api/factor_eval`。
  - 直接 in-process 复用 `quant.factor_eval.evaluate_many` + `build_ic_report`。
  - 复用 `quant.main.FACTOR_BUILDERS`，保证因子口径与回测**完全一致**。
  - 复用 `_resolve_codes()`（并集语义），并把签名改成接受任何有 `.pool/.codes` 的对象，
    让 `RunParams` 与 `FactorEvalParams` 共享同一解析路径。
  - 因子名先做白名单校验（防止 HTML 报告 render 时 KeyError）。
  - 拉不到价格的代码只给 warning 不挡报告。
  - NaN 走 `_json_safe()` 序列化成 None。
  - HTML 报告写到项目根目录 `factor_{uuid}.html`，返回文件名给前端 `/report/{name}`。

### 前端

- 新增折叠面板 **「因子有效性检验（IC / IR）—— 在跑回测前先确认因子是真有效」**。
- 6 个因子 chips（多选），默认勾选 momentum / reversal / low_volatility。
- 前向持有期、分组数输入。
- 一个「评估因子」按钮。
- 结果区展示：
  - 每个因子一张卡片：IR（核心）、IC 均值、t 值、正 IC 占比、多空收益、评价标签
  - 解读提示：IR 比 IC 均值更重要
  - 链接：「打开完整报告」跳转 `/report/{name}`（含累计 IC 曲线、IC 衰减、分组收益三图）
- 新增 CSS：`.chips` / `.ic-cards` / `.ic-card` 等，深色主题。

### 测试结果

`tests/test_web.py` 新增 13 项：
- 未知因子 / 空因子 / 没填池子 都返回可读 error
- 手动代码补零（858 → 000858）
- NaN 序列化安全
- 报告 HTML 落盘且三图齐全
- 数据缺代码只报 warning
- 因子评价 UI 元素齐全
- chips value 与后端 `_KNOWN_FACTORS` 严格一致

全套测试：**231 项通过**（218 → 231）。

### 实测

消费池（36 只，20230101~20260910，3 个因子，horizon=20）：

| 因子 | IR | IC均值 | 正IC | t | 多空/期 | 评价 |
|---|---|---|---|---|---|---|
| momentum | +0.093 | +0.0235 | 51% | +2.82 | +0.56% | 偏弱 |
| reversal | +0.037 | +0.0088 | 51% | +1.17 | +0.29% | 基本无效 |
| low_volatility | +0.392 | +0.0985 | 62% | +12.26 | +1.16% | 强有效 |

耗时 **12.3 秒**。

**这个结论很有价值**：
- 在「消费」这个池子上，**低波动因子是真正稳定的信号**；
- 动量因子 IC 均值还有 0.02+，但 IR 只有 0.09（不稳定）—— 说明时灵时不灵；
- 反转因子基本无效，不能单用。

这正好与 README 第 22 行「消费·reversal_20 / 沪深300·reversal_20」的稳健组合相呼应：
不是 reversal 强，而是**低波动/反转合成**后波动被压低了。

### 仍未解决

- P0：手动 push + 撤销 3 个泄露 PAT
- P1：默认结束日是否改到最新交易日
- P1：「一键今日清单」按钮
- P2：回撤熔断 / 止损
- P2：集成按模式指数加权
- P2：同步回测改异步
- P2：Chrome 无头截图做成回归测试
- P3：README 截图 / GIF、发布前端契约 skill

### 当前状态

- 工作区：3 个文件改动 + 运行记录未提交（`quant/web.py`、`quant/templates/index.html`、`tests/test_web.py`）
- 测试：**231 项通过**
- 服务：未启动（每次 Bash 会话结束都被自动收掉；启动方式见上文）

---

## 2026-09-14（续）多策略对比上页面

### 背景

P1 第二项。紧接因子评价：IC 告诉你「因子有没有效」，多策略对比告诉你
「把因子组合后，谁的收益/风险/夏普/超额最稳」。

### 后端

- 新增 `CompareParams` 入参模型（池子、手动代码、策略列表、区间、top_n、rebalance、buffer、基准等）。
- 新增 `POST /api/compare`。
  - 走 subprocess 调用 `python -m quant.main --compare --compare-strategies ...`。
  - 为什么走 subprocess：多策略对比是完整回测，需要 can_buy/can_sell、容量、基准、成本——
    这些 main.py 的 `--compare` 分支已经完整实现，复用它比 in-process 重造一套约束更安全。
  - 新增 `_build_compare_cmd()` + `_parse_compare_results()`：
    解析 `web_{uuid}_compare_results.csv` 与 `web_{uuid}_compare_report.html`。
- 更新 `.gitignore`：覆盖 `factor_*.html`、`web_*.html`、`web_*_holdings.csv`、
  `compare_*.{csv,html}`，防止测试产物误提交。

### 前端

- 新增折叠面板「多策略对比 —— 同一池子横向跑 7 个策略」。
- 8 个预定义策略 chips（多选），默认勾选 momentum_120/60、reversal_20、low_vol_60。
- 「对比策略」按钮。
- 结果区：按夏普排序的明细表（高亮第一行），含总收益/年化/夏普/最大回撤/波动/超额/IR；
  底部加解读提示。
- 复用 `fileLinks()` 提供「完整报告」「汇总 CSV」下载。

### 测试

`tests/test_web.py` 新增 7 项：
- `_build_compare_cmd` 参数翻译（含 --compare、pool/codes 并集、max-weight、默认策略省略）
- `/api/compare` 路由已注册
- UI 元素齐全
- 处理函数已定义
- chips value 与 `compare.PREDEFINED_STRATEGIES` 严格一致

全套测试：**238 项通过**（231 → 238）。

### 实测

消费池 36 只，20230101~20260910，3 策略，top_n=15，benchmark=sh000932：

| 策略 | 总收益 | 夏普 | 最大回撤 | 超额 | IR |
|---|---|---|---|---|---|
| low_vol_60 | -4.2% | +0.04 | -25.9% | +63.1% | 0.67 |
| momentum_120 | -12.4% | -0.08 | -27.4% | +49.1% | 0.56 |
| reversal_20 | -22.9% | -0.18 | -41.6% | +31.1% | 0.41 |

耗时 **14.3 秒**。

注意：虽然三个策略都是负收益，但基准（中证主要消费 000932）同期更差——
超额全为正、IR 都在 0.4 以上，说明这些策略相对池子本身有稳定优势。

### 仍未解决

同前一条记录：手动 push + PAT 撤销；CLI 里还缺 3+ 能力上页面（多池组合、风险归因、滚动回测/webhook）、
回撤熔断、指数加权集成、协方差权重、多因子权重自动寻优、行业覆盖率、main.py 拆分、异步化、
默认结束日、一键今日清单。

### 当前状态

- 工作区：4 个文件改动（`quant/web.py`、`quant/templates/index.html`、
  `tests/test_web.py`、`.gitignore`）+ 运行记录未提交
- 测试：238 项通过
- 服务：未启动

---

## 2026-09-14（续）用大白话给非技术用户跑一次实盘演示

### 背景

用户反馈「看不懂这个项目是做什么的，太多专业术语」「不关心结构，只关心怎么用，
能不能告诉我某只股票的买入卖出点」。

→ 关键认知：**用户要的是"这只票什么时候买/卖"，不是回测框架的架构说明。**
  但本项目是**回测工具不是预测工具**，只能给「历史倒带」的买卖点，不能给明天的建议。
  交付时必须把这个边界讲清楚，否则会被误读成荐股。

### 选股与规则

- 标的：**三一重工 600031**（用户选定，此前长期跟踪的工程机械标的）
- 规则：**反转 20 日**（用户选定）

### 踩到的两个坑

**坑 1：单只股票 + 反转规则 = 等于买入并持有，出不来买卖点**

第一次跑 `--codes 600031 --top-n 1`，结果：
```
调仓次数: 1   下单笔数: 1（买 1 / 卖 0）
2019-01-03 建仓 价格 6.86 股数 145700
```
只有一笔买入、从没卖出。**原因是 top_n=1 时没有"调出"机制**——
持仓股永远是自己的 top1，reversal 只在"要不要进"时起作用，进了就出不来。
所以「单只股票回测」在这个框架下**退化为买入并持有**，
**要给买卖点必须放进池子里跑**（让个股有"掉出前 N 名"的可能）。

> 这条值得记住：**以后凡是"我想看某只票的买卖点"，必须组池子跑，不能单跑。**

**坑 2：hs300 全池首次拉取太慢，16 分钟没跑完**

`--pool hs300 --codes 600031 --refresh-data` 后台跑了 16 分 34 秒仍未完成（300 只票全量）。
改用**工程机械小池**（6 只）后几秒完成。

> 教训：**演示/答疑场景优先用小池子**（同板块 5~8 只），别一上来拉 300 只。
> 大池首次拉取应该在没人等的时候后台慢慢做。

### 最终跑法

```bash
# 1) 补数据（6 只，几秒）
.venv/Scripts/python.exe -m quant.main --refresh-data \
  --codes "600031,000157,000425,601100,000528,603338" \
  --start 20200101 --end 20260910

# 2) 回测：工程机械 6 只池 + 反转 20 日 + top3 + 月度调仓 + 次日开盘成交
.venv/Scripts/python.exe -m quant.main \
  --codes "600031,000157,000425,601100,000528,603338" \
  --strategy reversal --reversal-lookback 20 \
  --top-n 3 --use-open --rebalance M \
  --start 20200101 --end 20260910 \
  --capital 1000000 --benchmark "" --out-prefix demo_gc_
```

### 结果（工程机械 6 只池 · 2020-01 ~ 2026-09）

| 口径 | 总收益 | 最大回撤 |
|---|---|---|
| 反转 20 日策略（top3 轮动） | **+45.0%** | −67.4% |
| 三一重工「一直拿着」 | +25.9% | −71.9% |

三一重工单独被交易 **19 个来回**（38 笔操作）：

- 胜率 **42.1%**（8 赚 / 11 亏）
- 平均每笔 **+2.04%**，最好 **+38.1%**，最差 **−29.0%**
- 平均持有 **65 天**

**这是典型的低胜率 / 高盈亏比特征**：亏的次数比赚的多，靠"一次赚的盖过几次亏的"。
实盘最难熬——连亏 3~4 笔是常态。这点必须在交付时明说。

### 交付物

`三一重工_反转20日_买卖点报告.html`（ECharts：价格走势 + 买卖点标记 + 逐笔明细 + 免责说明）
生成脚本 `_gen_600031_report.py`，中间数据 `_600031_chart.json`（均为 `_*` / 一次性产物）。

### 表达方式上的经验（给以后的自己）

- 同一份结果，讲「夏普 0.68 / 卡玛 0.21 / 年化换手 1082%」用户听不懂；
  讲「19 个来回，8 次赚 11 次亏，平均一笔赚 2%」用户立刻懂。
- **先给结论句，再给数字，最后给免责**。顺序反了等于没说。
- 涨红跌绿保持 A 股习惯。

### 仍未解决

同前（手动 push + PAT 撤销；CLI 还差多池组合/风险归因/滚动回测/每日监控 4 项上页面；
回撤熔断、指数加权集成、协方差权重、多因子权重寻优、行业覆盖率、
main.py 拆分、异步化、默认结束日、一键今日清单）。

### 当前状态

- 工作区：新增交付物 `三一重工_反转20日_买卖点报告.html` + 临时脚本 `_*`（已 gitignore）
- 测试：238 项通过（未改动代码）
- 服务：未启动

---

## 2026-09-14（续）单只股票「哪个策略更适合」——全样本结论被样本外推翻

### 背景

用户问：「如果我只想看三一重工更适合哪个策略，不需要类比其他股票池，该怎么做」。

### 第一个要讲清的认知：单只票比不了「选股策略」

选股策略（momentum/reversal/low_vol）本质是**横截面排序**——「在一堆票里挑跌得最惨/涨得最好的前 N 名」。
**一只票没法排序**，所以单只票跑选股策略全部退化为「买入并持有」，策略之间**没有区别**
（上一轮 `--codes 600031 --top-n 1` 实测只有 1 笔买入、0 笔卖出，已验证）。

→ 单只票真正能比的是**择时（timing）**，它看的是**这只票自己的时间序列**，不需要别的股票。

### 关键实现细节：`--timing-proxy pool` 让单只票能跑择时

`build_exposure(cfg, proxy=None, prices=...)` —— proxy 缺省时调 `pool_index(prices)`
构造池子等权组合。**池子只有 1 只票时，`pool_index` 就是那只票自己的净值**，
于是择时信号（均线/动量/双确认）就变成基于个股自身价格的时间序列信号。

> 这条要记住：**以后「单只票怎么跑择时」= `--codes XXX --top-n 1 --timing {mode} --timing-proxy pool`**。

### 实测：31 组择时组合，全样本 vs 样本外结论相反

```bash
# ① 全样本网格
.venv/Scripts/python.exe -m quant.main --codes 600031 --top-n 1 --use-open --rebalance M \
  --start 20200101 --end 20260910 --benchmark "" \
  --grid --grid-lookbacks 20 --grid-topn 1 --grid-buffers 0 \
  --grid-timing off,ma,momentum,dual \
  --grid-timing-lookbacks 40,60,90,120,200 --grid-timing-bands 0,0.02

# ② 样本外（8 折，训练 2 年 / 测试 0.5 年，49 秒跑完）
#    同上，把 --grid 换成 --walk-forward --fw-train 2 --fw-test 0.5
```

**全样本（回头看）** —— 基线不择时 +29.1% / 夏普 0.29 / 回撤 −73.0%

| 模式 | 跑赢基线 | 中位收益 | 夏普均值 | 回撤均值 |
|---|---|---|---|---|
| ma | 2/10 | 14.6% | 0.24 | −53.3% |
| **momentum** | **6/10** | **35.6%** | **0.33** | −56.8% |
| dual | 3/10 | 25.9% | 0.23 | −51.1% |

→ 表面结论：**动量最好**（最好单点 momentum(90日,带2%) +121.8%）。

**样本外（8 折事前选）** —— 选中模式：**ma × 5、dual × 3、momentum × 0**

| 做法 | 样本外总收益 | 平均夏普 | 最差折回撤 | 正收益折 |
|---|---|---|---|---|
| 自适应选参（argmax） | +23.9% | 0.30 | −25.1% | 50% |
| 邻域平滑（不踩尖峰） | −19.9% | −0.24 | −30.0% | 25% |
| 集成 K=3 | −3.9% | −0.04 | −24.9% | 38% |
| 集成 K=31（全部） | −8.7% | −0.16 | −23.2% | 50% |
| 全样本最优固定（事后选参） | +24.8% | 0.18 | −26.2% | 62% |
| 默认参数（不调参=不择时） | +2.8% | 0.16 | −30.8% | 50% |

### 三条必须写下来的判断

1. **「动量最好」是拟合出来的。** 全样本 6/10 跑赢、最好 +121.8%，
   但样本外 8 折**一次都没选中 momentum**。这是本项目到目前为止最干净的一个
   「事后选参幻觉」实例，适合拿来给新人讲为什么必须做 walk-forward。
2. **argmax 的 +23.9% 大概率是运气，不是策略有效。**
   判据：所有「不踩尖峰」的稳健做法（平滑 −19.9%、各集成度 −3.9%~−8.7%）**全是负的**。
   若策略真有稳定优势，稳健版本不该集体跑输。
   且收益高度集中在第 5、6 折（+22.9%、+15.8%，2024~2025 那波），**去掉这两折就不剩什么**。
3. **唯一稳的是「压回撤」。** 不择时最差折回撤 −30.8%，加择时后 −23%~−25%。
   这符合项目既有的结构性结论：**压回撤是择时唯一稳定的产出**（机制决定，不是预测准）。

### 表达上的注意

给非技术用户讲「哪个策略好」时，不能只报全样本最优（会被当成荐股）。
必须**同时给全样本和样本外两个视角**，并明确标注「诚实预期」是哪一行
（本例：集成 K=3 的 −3.9%，而不是 argmax 的 +23.9% 或全样本最优的 +121.8%）。

### 交付物

`三一重工_哪个策略更适合.html`：全样本 vs 样本外双栏对照、被选中次数条形图、
8 折逐段明细、样本外各做法汇总、复现命令、诚实结论与免责。

### 当前状态

- 工作区：新增交付物 `三一重工_哪个策略更适合.html` + 临时脚本/产物 `_*`、`demo_*`
- 测试：238 项通过（未改动代码）
- 服务：未启动

---

## 2026-09-14（续）三个案例对比：单只票 vs 一篮子组合

### 动机

上一条证明了「三一重工没有稳定适合的策略」。想知道这是不是**单只票的固有属性**——
于是在洽洽食品（另一只单票）和 12 只消费+蓝筹组合上跑**完全相同**的流程做对照。

### 三个案例（同一套：31 组择时 × walk-forward 8 折）

| 标的 | 全样本最优 | 样本外 8 折选中 | argmax | 集成K3 | 全样本最优固定 | 不调参 | 最差折回撤（不调参→集成K3） |
|---|---|---|---|---|---|---|---|
| 三一 600031 | 动量（6/10 跑赢） | ma×5 dual×3 **momentum×0** | +23.9% | −3.9% | +24.8% | +2.8% | −30.8% → −24.9% |
| 洽洽 002557 | 双确认（−6.2%） | **momentum×7** dual×1 | −42.8% | −36.5% | −12.3% | −53.3% | −35.4% → −24.0% |
| 12 只组合 | 事后选参 −14.9% | ma×4 momentum×2 **off×2** | −36.7% | −23.7% | −14.9% | **−4.9%** | −23.3% → **−11.8%** |

### 三条结论（可信度从高到低）

1. **「回头看最优」三次全被样本外推翻。**
   - 三一：回头看动量最强（最好 +121.8%），样本外动量 **0/8** 被选中
   - 洽洽：回头看双确认最好，样本外 **7/8 选动量**（方向完全相反）
   - 组合：事后选参 −14.9% vs 自适应 −36.7%，**高估 21.8pp**
   → 三次独立验证同一条：**任何「找到最优参数」的说法，验证前都不可信**

2. **唯一跨三案例都成立的是「压回撤」。**
   −30.8%→−24.9%、−35.4%→−24.0%、−23.3%→−11.8%，三个案例全部缩小 6~12pp。
   机制决定（空仓自然跌得少），不是预测准。**与项目既有的结构性结论一致。**

3. **压回撤的代价是收益。** 组合上「不择时」(−4.9%) 反而好过所有择时做法。
   所有「不挑尖峰」的稳健版本（平滑/各 K），三个案例里**没有一个是正收益**。

### 单只 vs 组合的差异（这是本轮最有价值的新发现）

- **组合没能让「哪个策略最好」变简单**：选中模式依然分散（ma4/momentum2/off2）
- **但组合让最坏情况明显变温和**：最差折回撤 −11.8% vs 单只的 −24%~−25%，**差一倍**
- 原因：12 只票互相分散，择时信号不用跟单只票的特质噪声搏斗

**注意一个反直觉点**：组合上 off（不择时）被选中 2 次，且「不调参」成绩最好（−4.9%）。
说明在这批大盘消费/蓝筹上，择时是**减分**的——跟上一条「三一上择时压回撤有效」不矛盾：
**压回撤有效 ≠ 提高收益**，这两件事必须分开讲。

### 方法论沉淀（可复用）

- **判断「某收益是真有效还是运气」的实用判据**：看「不挑尖峰」的版本还赚不赚钱。
  三一：argmax +23.9% vs 平滑 −19.9%（差 43.8pp）；组合：−36.7% vs −16.6%（差 20pp）。
  → **稳健版集体跑输 = 那个好看的数字主要是运气。** 这条已经三次验证，可以当规则用。
- **给非技术用户讲「哪个策略好」**：必须同时给全样本和样本外两个视角，
  并明确标注「诚实预期」是哪一行（集成 K=3），而不是 argmax 或全样本最优。

### 交付物

`单只票vs组合_三个案例对比.html`：全样本 vs 样本外对照表、样本外各做法成绩、
压回撤对比条形图、单只 vs 组合差异表、复现命令。

### 当前状态

- 工作区：新增交付物 `单只票vs组合_三个案例对比.html` + 临时脚本/产物 `_*`、`demo_*`
- 测试：238 项通过（未改动代码）
- 服务：未启动

---

## 2026-09-14（续）把整套流程做成一键脚本 + 修两个真 bug

### 动机

前面三轮都是「我手工敲命令 → 分析结果 → 写报告」。用户下次想换只票还得再来一遍。
所以把流程固化成脚本，用户自己就能跑。

### 交付

- **`一键策略对比.py`** —— 给股票代码，自动跑完
  「补数据 → 全样本 31 组 → 样本外 8 折 →（多只票）选股因子对比 → 出 HTML 报告」。
- **`一键策略对比.bat`** —— 双击运行，交互式询问股票代码（GBK + CRLF，沿用 `启动网页.bat` 的约定）。
- **README** 新增「一键策略对比」章节（含三个案例的翻车对照表 + 判据）。

用法：
```bash
.venv/Scripts/python.exe 一键策略对比.py 600031
.venv/Scripts/python.exe 一键策略对比.py 002557,600519,000858,600887,603288,600031 --top-n 3
```

### 关键设计

1. **固定跑两步**：`--grid`（全样本）+ `--walk-forward`（样本外）。
   只跑前者得到的「最优参数」已被三个案例证明是拟合的，所以脚本不允许跳过后者。
2. **固定选股因子 `reversal --reversal-lookback 20`**：
   单只票时选股因子对结果无影响（top_n=1 永远选中它自己），但固定住可保证口径可复现。
3. **报告里标出「诚实预期」= 集成 K=3 那一行**，而不是 argmax 或全样本最优。
4. **免责写死在报告里**：不是预测、不是买卖建议 + 5 条限制。

### 踩到并修掉的两个真 bug

**bug 1：文件名前缀冲突 → 读到上一次的过期结果（最危险，因为看不出来）**

初版 `prefix = f"_auto_{codes[0]}_"`，于是
- 单只 `002557` → `_auto_002557_*`
- 多只 `002557,600519,...` → 也是 `_auto_002557_*`（撞名）

多只票那次 600519 拉数据失败、回测没跑完，脚本却**读到了上次单只跑留下的旧 csv**，
出了一份「文件名叫 `002557_组合_策略对比.html`、内容却是单只结果」的报告。

修复：
```python
key = hashlib.md5(codes_s.encode()).hexdigest()[:6]
prefix = f"_auto_{codes[0]}_{key}_"
# 跑之前先清掉同前缀旧文件 —— 这样本次若失败，后面找不到文件就会报错退出
for f in ROOT.glob(f"{prefix}*"): f.unlink()
```
并对 grid / walk-forward 各加一道「产物不存在就 sys.exit(1)」的闸门。

> **教训（可推广）**：任何「跑批 → 读文件 → 出报告」的脚本，
> **前缀必须能区分入参，且跑之前要清旧文件**。
> 否则失败时会静默拿上一次的结果出报告——这类错误没有任何报错，只有仔细对数据才能发现。

**bug 2：默认结束日用「今天」→ 必然失败**

`end = 今天(20260914)` 时，接口当天还没数据，
`load_panel` 去拉 `20260912~20260914` 直接 `RuntimeError`：
```
新浪接口在 20260912~20260914 内无数据（可能停牌/退市）
```
整个回测跑不起来。

修复：拆成两个日期——
```python
refresh_end = 今天           # 补数据时尽力往前够
end         = detect_end()   # 回测用缓存里这批票共同的最新日期（取 min，最保守）
```
`detect_end()` 读本地缓存各票最后一行日期取最小值。

> 这也是 Web 控制台那个「结束日期默认 20240909」老问题的同一类根源：
> **end 一旦晚于实际数据，就不是「少算几天」，而是整个跑不起来。**

### 顺带修的第三个问题：.gitignore 漏了 csv/html

`.gitignore` 里只有 `/_*.py` `/_*.js` `/_*.json`，
而新脚本会吐 `_auto_*.csv` / `_auto_*.html` 中间产物 → `git status` 一片垃圾。
改成 `/_*`（覆盖全部扩展名）+ `/demo_*`。

> 记忆里写的是「根目录 `_*` 临时文件统一忽略」，实际只忽略了三种扩展名。
> **记忆和代码不一致时，以代码为准**——这次是 grep 才发现的。

### 实测

- 单只 002557：1 分 29 秒，结果与手工跑一致（全样本最优 双确认 平均 −6.2%）
- 6 只组合（top3）：3 分 39 秒，四项全部跑通
  - 全样本最优：双确认（平均 +4.5%）
  - 样本外选中：均线 5、不择时 2、双确认 1
  - 诚实预期（集成 K3）−25.3% vs 不调参 −25.9%
  - 选股因子对比（多只票专有）：momentum_120 +12.7%/夏普0.21 领先，
    low_vol_60 +12.4%/0.20 次之，ma_trend −57.9% 垫底

### 当前状态

- 工作区：`一键策略对比.py` / `一键策略对比.bat` / README / .gitignore / 运行记录 + 3 份 HTML 交付物
- 测试：238 项通过（未改动 `quant/` 与 `tests/`）
- 中间产物 `_auto_*`、`demo_*` 已进 .gitignore

---

## 2026-09-14（续）修掉「打开就跑、拿到两年前清单」这个最痛的坑

### 问题

Web 控制台（用户的**主要入口**）结束日期字段硬编码 `value="20240909"`。
用户不改就点「生成调仓清单」，拿到的是「假如当时是 2024-09-09 该买什么」——
回测照样跑通、清单照样漂亮，但完全不是当前的操作建议。

原本有「用最新数据」按钮能修正，但**需要用户主动点**，容易被忽略。
上一轮在一键脚本里已经用 `detect_end()` 解决了，网页这边还是老样子。

### 改法

前端 `renderFreshness()` 里加首次自动对齐：

```javascript
if (!autoAligned && d.latest && !d.usable) {
  autoAligned = true;
  document.forms.runForm.end.value = d.latest.replace(/-/g, '');
  checkFreshness();   // 用新日期再体检一次
  return;
}
```

**只做一次**（`autoAligned` 标志）：之后用户可能故意填历史日期做回放，不该反复覆盖。
`autoAligned` 已置 true，所以重跑体检不会递归。

顺带把 placeholder 改成「打开页面会自动对齐到数据最新日」。

### 实测

无头 Chrome 加载 `http://127.0.0.1:8765/`，体检条文案：
```
✓ 数据已更新到 2026-09-11，与结束日 2026-09-11 一致
```
→ 证明 `20240909` 已被自动替换为 `20260911`。

### 验证过程中踩到的两个环境坑（都不是代码问题）

**坑 A：公司代理会拦截本地请求，返回 502**
`HTTPS_PROXY=http://127.0.0.1:59277` 连 `127.0.0.1:8765` 也吞。
一开始 urllib 健康检查超时/502，误以为服务没起来。
- Python 修法：`urllib.request.build_opener(urllib.request.ProxyHandler({}))`
- Chrome 修法：加 `--no-proxy-server`

> 和「git push 被代理拦」是同一个代理，但表现完全不同（502 vs 拒绝隧道），容易误判。

**坑 B：后台服务会被会话回收**
先发现端口被一个挂死的 `quant.web`（PID 17360）占着（只 LISTENING 不响应）。
杀掉后用 PowerShell `Start-Process -WindowStyle Hidden` 重启，
紧接着 `Invoke-WebRequest` 就 10061——**PowerShell 也逃不掉会话回收**。
可靠做法：**同一条 Bash 命令内** `(python -m quant.web &) && sleep 9 && 验证`。

**坑 C：`--dump-dom` 看不到 JS 改的值**
它输出的是 HTML 初始 attribute（`value="20240909"`），
JS 改的是 DOM property，dump 里仍是旧值。
要证明改动生效，得看**后端实际收到的值**或页面状态文案（体检条那句「与结束日 ... 一致」）。

### 当前状态

- 工作区：干净（除运行记录），最新提交 `14ef891`（前端自动对齐）、`45aab54`（一键脚本）
- 测试：238 项通过（未改动 `quant/` 与 `tests/`，只改了 `quant/templates/index.html`）
- 服务：当前已在跑（本次会话内启动）

---

## 2026-09-14 · 在 VSCode 里看第三方库源码（环境备忘，非代码改动）

起因：用户想在 VSCode 里看 `partition` 的实现。用 `.venv/Scripts/python.exe` 实测确认了几个路径，
顺便把「哪些能跳、哪些跳不动」摸清楚了，以后查源码直接看这条。

**前提：VSCode 必须选 `.venv` 解释器**
`Ctrl+Shift+P` → `Python: Select Interpreter` → `.\.venv\Scripts\python.exe`。
不选会跳到系统 `C:\Python312`，那里的 numpy/pandas 版本与项目不一致，看到的源码是错的。
（实测 `inspect` 就是用系统 Python 的，报错栈里出现 `C:\Python312\Lib\inspect.py`。）

**实测结论（numpy 2.5.3 / pandas 版本见下）**

| 目标 | F12 结果 | 能不能读到实现 |
|---|---|---|
| `Series.nlargest` | 跳到 `.venv\Lib\site-packages\pandas\core\series.py` **第 4018 行** | 能，纯 Python |
| `np.partition` / `np.argpartition` | 跳到 `numpy\_core\fromnumeric.pyi` | **不能**，只有签名 |

**为什么 np.partition 看不了**：numpy 2.x 里它是 C 写的 `_ArrayFunctionDispatcher`，
`inspect.getsourcefile` 直接报
`TypeError: ... got _ArrayFunctionDispatcher`（拿不到任何 Python 源码）。
真正算法在 C 源码 `numpy/_core/src/npysort/` 下，只能上 GitHub 看。

**实用跳转键**：`F12` 跳入 / `Ctrl+点击` / `Alt+F12` 小窗预览（不离开当前文件）/ `Alt+←` 返回 /
`Ctrl+T` 全局搜符号 / `Ctrl+P` 搜文件名。跳进去的是 site-packages 里的文件，**只读，别改**。

**推荐学习路线**：想理解「找第 K 大值」的算法，别死磕 numpy 的 C，
从 `Series.nlargest`（series.py:4018）往下读，它内部就是调 `np.argpartition`——
先读 Python 层的调用方式，比直接看 C 划算得多。

---

## 2026-09-14（本轮）：回撤熔断 + 一键今日清单 + 自选组合预设

### 1. 回撤熔断 `--dd-brake`（P2，新能力）

**语义**：与择时是两件事——择时看「市场趋势好不好」，熔断看「自己亏了多少」。
择时需要预测能力，熔断不需要。参数：`--dd-brake 阈值` / `--dd-brake-action 降到几成`
/ `--dd-brake-resume 反弹多少才回来`（默认阈值一半，滞回带）。

**引擎改动**：`backtest.run()` 是向量化的，而熔断是路径依赖的（仓位影响净值、
净值又决定仓位）。把「权重 → 净值/成本」那一段抽成内部 `_simulate(w)`，
熔断时对它做定点迭代（上限 10 次，不收敛会在 `metrics.dd_brake_converged` 标 False）。
新增 `brake_exposure_from_equity()` 状态机（独立函数，好单测）+ `brake_kwargs()`
（与 `cost_kwargs` 并列，六个入口 main/grid/compare/combine/rolling/daily 全部接上，
防止「主回测有熔断、网格没有」的口径分裂）。

**踩的三个坑（都已修，`tests/test_dd_brake.py` 逐条锁定）**——这类状态机写错了
不报错，只表现为「净值怎么一直平的」，从结果极难反推：

1. **清仓后净值冻结 → 永久空仓。** 降到 0 仓后净值不再变化，回撤永远停在触发点，
   恢复条件永远不成立。实测「跌 10% 清仓」1623 天里熔断 1606 天、收益 −13%。
   → 恢复必须看**影子净值**（不熔断会走成什么样），不看自己那个冻结的净值。
2. **恢复判据不能是「距历史高点还有多远」。** 这个池子 6 年跌 67%，影子净值相对
   历史高点永远回不来，等于还是永不恢复。→ 改成「从熔断后的低点反弹了多少」。
3. **恢复后立刻再次触发。** 清仓期间净值冻结在 −30%，不重置高点基准的话，
   恢复只维持一天。→ 每次熔断后重新起算观察起点。

修的过程中还发现判断顺序问题：`dd <= -threshold` 必须只在**未熔断**时检查，
否则清仓后它恒成立，恢复分支永远走不到（这就是坑 1 的第二层原因）。

实测踩过一次浮点边界：`1.0 → 0.8` 算出来是 `-0.19999999999999996`，严格比较
「刚好差一点没触发」，已加 `1e-12` 容差。

**实测结论（工程机械池 6 只，反转 20 日，top3）——机制有效，但参数不可迁移**：

| | 训练段 2020–2023（大跌） | 验证段 2024–2026（上涨） |
|---|---|---|
| 基线（不熔断） | −10.9%，回撤 −67.4% | **+56.9%**，回撤 −27.0% |
| 20 组熔断里跑赢基线的 | 16 组 | **0 组** |
| 训练段第 1 名 | +31.1%，回撤 −46.2% | 未触发，等于基线 |
| 训练段倒数第 1 名 | −12.3% | 触发 31%，**−6.0%** |

- 压回撤是真的（−67.4% → −46.2%）；
- 但「跑赢基线」是幻觉：训练段是大跌段，任何减仓都显得有用；换成上涨段后，
  熔断要么不触发（白交参数复杂度），要么触发了就严重拖累（差 63pp）；
- 训练段排名与验证段**零相关**——验证段 6 组里 5 组压根没触发，唯一触发的
  恰恰是训练段最后一名；
- 最危险的失败模式是阈值太紧：15% 那档训练段熔断占比 28~38%，长期空仓垫底。

**走路时要用的**：阈值别低于 25%，且必须先在没见过的时间段验证过。

**walk-forward 验证不了熔断**：每折测试段只有半年、净值从 1.0 重开，
最深回撤才 −25%，够不到 30% 阈值，两次跑出来结果逐位相同（一次都没触发）。
时间分割（前 4 年选、后 2.5 年验）才是这里该用的方法。

### 2. 一键今日清单（Web）

结束日期字段默认 20240909，用户不改就点「生成调仓清单」，拿到的是「假如当时是
2024-09-09 该买什么」的历史回放。上一轮做了「页面打开时自动对齐」，这轮把保险
做到后端：新增 `plan_today` 开关，勾上就先补数据、再把 end / plan_date 一起对齐到
数据最新交易日。

**基准日取各票缓存日期的最大值**（与页面体检条同口径），否则会出现「数据到 09-11、
清单基准日 09-01」打架的提示；个别停牌者由 main.py 的「无法计算」告警单独兜住。
数据比今天落后 7 天以上会单独告警。

### 3. 自选组合预设（Web）

新增 `quant/presets.py`：把常用的一篮子股票存成名字，下拉直接选。
内置 5 组（老登组合 / 消费白马 / 工程机械 / 白酒 / 金融蓝筹），
用户自定义存 `my_presets.json`（已 gitignore）。
填入预设时会提示「股票池非空则取并集」，避免以为只跑预设那几只。

**顺带修契约检查器**：`test_frontend_only_calls_registered_api` 只认 `get|post`，
新增的 `DELETE /api/presets/{name}` 被判成「未注册」；改为也收 `delete|put`，
并把 `{name}` 放宽成通配后整体匹配（前端是「前缀 + encodeURIComponent」拼接调用的）。

### 4. 环境坑（本轮新踩）

- `bc` 在这个 Git Bash 里不存在，shell 里算百分比会静默失败（只影响显示，不影响回测）。
- `--benchmark ""` 放在 shell 变量展开的命令行里会被吞掉，报
  `argument --benchmark: expected one argument`；要在末尾显式写 `--benchmark ""`。
- metrics.csv 是三列 `metric,label,value`，不是两列；按两列读会全读成 0。

### 当前状态

- 测试：**298 项通过**（新增 `tests/test_dd_brake.py` 16 项 + 一键清单 10 项 + 预设 27 项）
- 工作区：本轮改动待提交
- 服务：未启动（Bash 会话结束会被收；用 `.venv/Scripts/python.exe -m quant.web`
  或双击 `启动网页.bat`）
