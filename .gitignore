# ---------- Python ----------
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
dist/
build/
*.whl

# ---------- 虚拟环境 ----------
.venv/
venv/
env/
ENV/

# ---------- 缓存的股票/指数数据 ----------
# data/ 里是 akshare 拉下来的日线 CSV 和成分股缓存，几百 MB，
# 公开仓库不要上传。新 clone 后跑命令会自动重新拉取。
data/
!data/.gitkeep

# ---------- 回测临时输出（一次性 CSV/HTML）----------
web_*.csv
today_*.csv
today_*.html
attr_*.csv
attr_*.html
grid_*.csv
grid_*.html
cmp*.csv
cmp*.html
cmb*.csv
cmb*.html
roll_*.csv
roll_*.html
daily_*.html
*_today_plan.csv
*_rebalance_plan.csv
*_annual_metrics.csv
*_equity_curve.csv
*_benchmark_curve.csv
*_metrics.csv
*_daily_detail.csv
*_report.html
*_grid_report.html
*_compare_report.html
*_combine_report.html
*_combine_correlation.csv
*_attribution.csv
*_rolling_curve.csv
*_rolling_metrics.csv
*_combine_equity.csv
# 样本外验证 / 因子有效性 / 多策略对比（P1 新增的输出）
*_wf_folds.csv
*_wf_summary.csv
*_wf_report.html
*_factor_eval.html
factor_*.html
web_*.html
web_*_holdings.csv
compare_*.csv
compare_*.html

# ---------- 用户私有 ----------
my_holdings*.csv
daily*.json

# ---------- IDE / 编辑器 ----------
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# ---------- 日志 / 环境 ----------
*.log
.env
.env.local
.env.*.local

# ---------- 工具产物 ----------
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/
# pytest 默认往系统 Temp 写临时目录，沙箱环境里常被拦；
# 用 --basetemp=.pytest_tmp 把它落到项目内，记得别提交
.pytest_tmp/
# 一次性的验证 / 检查脚本与临时输出（不是项目代码，删掉无妨）
# 项目正经文件都在 quant/ 、tests/ 下，根目录不用 _ 开头的名字
# ⚠️ 必须覆盖全部扩展名，不能只写 .py/.js/.json：
#    一键策略对比.py 等脚本会往根目录吐 _auto_*.csv / _auto_*.html 中间产物，
#    只忽略三种扩展名的话这些会漏进来，git status 一片垃圾。
/_*
# 根目录 demo_ 开头的是一次性演示产物（跑示例/答疑时生成）
/demo_*
# 一键策略对比生成的报告（每次跑都会覆盖同名文件，不入库）
*_策略对比.html